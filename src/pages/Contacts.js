import React from 'react';
import Layout from "../components/Layout/layout";

const Contacts = () => {
    return (
        <Layout>
            <div>
            </div>
        </Layout>
    );
};

export default Contacts;